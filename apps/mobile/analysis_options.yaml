include: package:flutter_lints/flutter.yaml

analyzer:
  # riverpod_lint runs via `dart run custom_lint` (also a ci-mobile step), not
  # as an analyzer plugin: the legacy `plugins:` key is deprecated in Dart 3.13
  # and trips --fatal-infos. IDE integration is a per-developer choice.
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    invalid_annotation_target: ignore
  exclude:
    - build/**
    - android/**
    - ios/**
    # Generated by build_runner; not ours to lint. They ARE committed, so a
    # clone builds without codegen; ci-mobile regenerates and diffs to catch
    # a stale checkin.
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - web/**

linter:
  rules:
    - always_declare_return_types
    - prefer_const_constructors
    - prefer_const_declarations
    - prefer_final_locals
    - avoid_print
    - require_trailing_commas
