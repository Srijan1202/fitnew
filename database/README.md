# database/

Reserved by spec §29.

- `migrations/` — drizzle-kit output, forward-only, SQL in git and reviewable (§25).
- `seeds/` — `exercises.json` (Phase 3), `foods.json` (Phase 7), `dev-users.json`.

Empty at Phase 0: the first migration is `users` in Phase 1.
